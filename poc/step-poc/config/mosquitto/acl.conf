# ACL Configuration for Certificate Management PoC
# Allow all authenticated users full access to all topics

# Allow specific user (from certificate CN)
user centrifuge-001
topic readwrite #

# Allow any user pattern (fallback)
pattern readwrite #
pattern read #
pattern write #
